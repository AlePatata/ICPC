#include <bits/stdc++.h>
using namespace std;
typedef long long ll;


int main() {
	int t; cin >> t;
	for (int l = 0; l < t; ++l)v{
		int n; cin >> n;
		for (int i = 0; i < n; ++i) {
			int ai; cin >> ai;
		}
		int q; cin >> q;
		for (int i = 0; i < q; ++i) {
			int l, u; cin >> l >> u;
		}
	}
	return 0;
}