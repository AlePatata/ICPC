#include <bits/stdc++.h>
using namespace std;
typedef long long ll;

int main(){
// hacer un hash que te diga el prefijo de la palabra magica en cada posicion, 
// la dp[i] es verdadera si los primeros i caracteres 
// se puede armar mi palabra magica
	/* Luego se itera por todas las palabras chicas ta que si para la dp[i]
	la dp[i - largo de la palabra a iterar] es true */
	return 0;
}